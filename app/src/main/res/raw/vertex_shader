/* Creating vertex shader
* Vertex shader: is responsible for handling a 3D object's vertices
* Fragment shader: responsible for coloring the 3D object's pixels */

attribute vec4 position;
uniform mat4 matrix;

void main() {
    gl_Position = matrix * position;
}